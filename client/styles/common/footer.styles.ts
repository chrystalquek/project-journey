import { CSSProperties } from 'react';

const footer: CSSProperties = {
  textAlign: 'center',
  bottom: 0,
  position: 'absolute',
  width: '100%',
  paddingBottom: 20,
};

export default {
  footer,
};
