import { Layout, Menu, Button, Divider, Row, Col, Card} from 'antd';

const { Header, Content, Footer } = Layout;

export default function Auth() {
  return (
      <Footer style={{ textAlign: 'center' }}>Journey ©2021 Created by DSC NUS</Footer>
  )
}
